name: debug-backend-build
on: [push, workflow_dispatch]
jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build backend Docker image (same as Render)
        run: docker build -f backend/Dockerfile -t debug-backend backend
